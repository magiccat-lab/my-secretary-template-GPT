# 記入後は権限を絞ってください: chmod 600 .env
# このファイルは plaintext でシークレットを保持します。
# .gitignore で除外済みですが、絶対に commit しないよう注意。
# 漏洩したら docs/bootstrap.md §2（シークレット漏洩時のローテーション）を参照。
#
# このファイルを .env にコピーして値を埋めてください:
#   cp .env.template .env
#   chmod 600 .env
#
# TODO がついている値は必須。オプションのセクションは空でもOK。

# ===== Discord =====
# Bot トークンは ~/.claude/channels/discord/.env に保存します（プラグインが要求）。
# リポジトリのスクリプトもそのファイルから読むので、ここに複製する必要はありません。
# 詳細は SETUP.md セクション D を参照。

DISCORD_USER_ID=                         # TODO: 自分の Discord ユーザーID（定期メッセージのメンション用）

# チャンネルID（Discord → 設定 → 詳細設定 → 開発者モード → チャンネル右クリック → IDをコピー）
DISCORD_CHANNEL_RANDOM=                  # TODO: メインのチャットチャンネル — デイリーリマインドやアドホックなメッセージで必須
DISCORD_CHANNEL_MAIL=                    # TODO: メール通知の宛先チャンネル（Gmail モニターの通知先。未設定なら random に流れる）

# ===== Webhook サーバー =====
WEBHOOK_PORT=8781
WEBHOOK_TOKEN=                           # 生成: python3 -c "import secrets; print(secrets.token_hex(32))"

# ===== Google（Calendar / Gmail / Sheets / Drive / Docs / Forms で同じトークンを共有） =====
# integrations/google/reauth.py を1回実行して token.json を作ってください。
GOOGLE_TOKEN_PATH=integrations/google/token.json
GCAL_CALENDAR_ID=primary                 # または your-calendar@gmail.com

# ===== タスクストレージ（Google Sheets バックエンド、オプション） =====
# 空にするとローカル JSON（デフォルト）を使用。セットすると双方向同期が有効になる。
TASK_SHEET_ID=

# ===== Notion（タスク同期 + Wishlist、オプション） =====
# 詳細は SETUP.md G2 / docs/notion.md を参照。
# Integration 作成: https://www.notion.so/my-integrations
NOTION_TOKEN=                            # secret_xxxx... 形式の Internal Integration Secret
NOTION_DB_TASKS=                         # Tasks DB の URL から ?v= 直前 32 文字
NOTION_DB_WISHLIST=                      # Wishlist DB（同じ要領で取得）
NOTION_DB_LOG_LIBRARY=                   # Log Library DB（Discord ログ・まとめ資料の集約先。同じ要領で取得）

# ===== フィーチャーフラグ =====
# false の機能は cron 行をコメントアウトしたまま、true にするとセットアップ後に有効化
# Gmail モニターは GOOGLE_TOKEN_PATH の 1 アカウントを監視する。別アカウントのメールも
# 拾いたい場合は (a) そのアカウントから監視アカウントへ転送設定する か (b) そのアカウントの
# token を別途用意してモニターを増やす（詳細は SETUP.md C3 / docs/google.md）。
GMAIL_ENABLED=false
GCAL_REMIND_ENABLED=false

# ===== オプションの API =====
BRAVE_API_KEY=                           # Brave Search（リサーチ・Web検索用）。取得: https://api-dashboard.search.brave.com/ で無料プラン登録 → API Keys
